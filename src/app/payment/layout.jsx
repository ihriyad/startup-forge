import React from 'react';

export const metadata = {
  title: "Payment | StartupForge",
  description: "StartupForge Payment system",
};

const PaymentPageLayout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default PaymentPageLayout;